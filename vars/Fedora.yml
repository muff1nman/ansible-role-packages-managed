---
rpm_keys:
 - "RPM-GPG-KEY-rpmfusion-nonfree-fedora-{{ ansible_distribution_major_version }}"
 - "RPM-GPG-KEY-rpmfusion-free-fedora-{{ ansible_distribution_major_version }}"
 - "microsoft.asc"
 - "RPM-GPG-KEY-hashicorp"
package_repos:
 - google-chrome.repo
 - copr-demaria-desktop.repo
 - rpmfusion2.repo
 - vscode.repo
 - hashicorp.repo
package_tmpl_repos: []
